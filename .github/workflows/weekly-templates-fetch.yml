name: Weekly Full Templates Fetch

on:
  schedule:
    # Run every Sunday at 04:00 UTC (before daily build at 06:00)
    - cron: '0 4 * * 0'
  workflow_dispatch: # Allow manual trigger

jobs:
  fetch-all-templates:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Templates ~16 min, nodes ~2 min, add buffer

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch all templates from n8n API
        run: npx tsx scripts/fetch-all-templates.ts

      - name: Fetch all nodes usage data from n8n API
        run: npx tsx scripts/fetch-all-nodes.ts

      - name: Update nodes history
        run: npx tsx scripts/update-nodes-history.ts

      - name: Commit updated data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add public/data/all-templates-data.json public/data/all-nodes-data.json public/data/nodes-history.json
          git diff --staged --quiet || git commit -m "Update full templates, nodes data, and history (weekly) [skip ci]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
